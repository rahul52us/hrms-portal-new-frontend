"use client";

import { Box, Text } from "@chakra-ui/react";
import { observer } from "mobx-react-lite";

const Home = observer(() => {
  return (
    <Box>
       <Text>Home</Text>
    </Box>
  );
});

export default Home;
